import 'package:flutter/material.dart';

class NewPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Post Project')),
      body: Center(
        child: Text('This is the Review and Post page'),
      ),
    );
  }
}
